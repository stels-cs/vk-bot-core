const Core = require('./src/core')
const {Keyboard, DefaultBtn, NegativeBtn, PositiveBtn, PrimaryBtn, DropKeyboard, GetKeyboard} = require('./src/keyboard')
const {PhotoFile, AudioFile, DocFile} = require('./src/tools')

module.exports = {
	Core,
	Keyboard,
	DefaultBtn,
	NegativeBtn,
	PositiveBtn,
	PrimaryBtn,
	DropKeyboard,
	GetKeyboard,
	PhotoFile,
	AudioFile,
	DocFile
}